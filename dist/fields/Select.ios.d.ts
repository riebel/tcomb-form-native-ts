import type { SelectTemplateProps } from '../types/template.types';
declare const SelectIOS: <T>({ options, value, onChange, nullOption, disabled, hidden, stylesheet, hasError, label, help, error, ...rest }: SelectTemplateProps<T>) => import("react/jsx-runtime").JSX.Element | null;
export default SelectIOS;
